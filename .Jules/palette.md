## 2025-06-01 - Inline Success State & A11y
**Learning:** Replacing native browser `alert()` with an inline success state using React state and simple conditional rendering provides a much more integrated and modern feel without exceeding line limits.
**Action:** Use conditional rendering for form success states and always link labels with `id`/`htmlFor` for better accessibility.
