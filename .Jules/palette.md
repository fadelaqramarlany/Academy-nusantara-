# Palette's Journal - Academy Nusantara

## 2025-05-15 - Initial Audit
**Learning:** Found several accessibility gaps: missing `aria-label` on icon-only buttons/links, and missing `htmlFor`/`id` associations in forms. The use of `alert()` for form feedback is also a UX anti-pattern for a modern interface.
**Action:** Plan to implement better form accessibility and feedback in `pages/Contact.tsx`.
