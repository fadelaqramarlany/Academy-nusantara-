## 2026-03-29 - Improve Registration Form Accessibility and Tactile Feedback
**Learning:** Initializing the palette journal to track UX and accessibility improvements. Form labels should always be linked to inputs using `id` and `htmlFor` for better screen reader support and hit area. Tactile feedback on buttons (e.g., `active:scale-95`) provides immediate visual confirmation of a click.
**Action:** Always check for unlinked form labels and missing tactile feedback on primary action buttons.
